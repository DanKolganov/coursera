# ============================================================
# Зависимости проекта AVSR
# Python 3.10+. Версии зафиксированы, чтобы не сломалось через месяц.
# ============================================================

# --- Ядро PyTorch (на Colab/Kaggle обычно уже установлено,
#     ставить вручную НЕ нужно — используем то, что есть в облаке) ---
# torch==2.3.0
# torchaudio==2.3.0
# torchvision==0.18.0

# --- HuggingFace: предобученные модели (Whisper) и удобные датасеты ---
transformers==4.41.2
datasets==2.19.1
accelerate==0.30.1
huggingface_hub==0.23.0

# --- Аудио ---
librosa==0.10.2          # удобный анализ аудио, ресэмплинг
soundfile==0.12.1        # чтение/запись wav
sounddevice==0.4.6       # захват с микрофона в реальном времени (для демо)

# --- Видео и обработка изображений ---
opencv-python>=4.9
mediapipe>=0.10.18       # детекция лица и ключевых точек губ
                         # (>=0.10.18 нужно для Python 3.12 на свежем Colab)
protobuf>=5.26           # фикс конфликта с tensorflow на новых Colab/Kaggle
Pillow>=10.3

# --- Метрики качества распознавания ---
jiwer==3.0.4             # WER, CER

# --- Конфиги и логирование ---
PyYAML==6.0.1
omegaconf==2.3.0         # удобные иерархические конфиги
tqdm==4.66.4             # прогресс-бары
tensorboard==2.16.2      # альтернатива wandb для логов
# wandb==0.17.0          # раскомментировать, если хочешь онлайн-логи (нужна регистрация)

# --- Численные вычисления, утилиты ---
numpy==1.26.4
scipy==1.13.0
pandas==2.2.2
einops==0.8.0            # удобные reshape-операции для тензоров

# --- Демо-интерфейс ---
gradio==4.31.5           # веб-морда для запуска модели в браузере

# --- Опционально: ускорение Whisper при инференсе ---
# faster-whisper==1.0.2  # ставить, только если будем оптимизировать

# --- Тестирование (опционально) ---
pytest==8.2.0
