"""
Инференс в реальном (или почти реальном) времени.

План: см. README, фаза 5.
  Audio: sounddevice callback → буфер 0.5 сек → мел-спектрограмма.
  Video: cv2.VideoCapture в отдельном потоке → губы → буфер.
  Каждые 0.5 сек: прогон через модель, декодирование, вывод текста.

Для курсовой минимум: офлайн-демо на записанном видео (показать RTF и WER).
Веб-камера — bonus, если останется время.
"""
from __future__ import annotations

# TODO(day-21): RealtimeAVSR — класс, оборачивающий модель в потоковый пайплайн.
